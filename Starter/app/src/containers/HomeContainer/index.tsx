import * as React from 'react';
import { View, Text, ScrollView } from 'react-native';

export const HomeContainer = () => {
  return (
    <ScrollView>
      <View>
        <Text>hi there</Text>
      </View>
    </ScrollView>
  );
};
